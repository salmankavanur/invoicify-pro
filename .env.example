# =============================================================================
# INVOICIFY PRO - ENVIRONMENT CONFIGURATION TEMPLATE
# =============================================================================
# Copy this file to .env.local and fill in your actual values
# Command: cp .env.example .env.local (Linux/Mac) or copy .env.example .env.local (Windows)
# =============================================================================

# -----------------------------------------------------------------------------
# REQUIRED: AI SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# Google Gemini AI API Key - REQUIRED for AI features
# Get your key from: https://makersuite.google.com/app/apikey
# Without this key, AI features will not work
GEMINI_API_KEY=your_gemini_api_key_here

# -----------------------------------------------------------------------------
# APPLICATION CONFIGURATION
# -----------------------------------------------------------------------------
NODE_ENV=development
VITE_APP_VERSION=1.0.0

# -----------------------------------------------------------------------------
# SERVER CONFIGURATION
# -----------------------------------------------------------------------------
# Port for development server (default: 3000)
VITE_PORT=3000

# Host for development server (0.0.0.0 allows external access, localhost for local only)
VITE_HOST=0.0.0.0

# -----------------------------------------------------------------------------
# GOOGLE SHEETS INTEGRATION (Optional but Recommended)
# -----------------------------------------------------------------------------
# Google Sheets Web App URL from Apps Script deployment
# Leave empty to use LocalStorage only (no cloud backup)
# Example: https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec
VITE_GOOGLE_SHEET_URL=

# Auto-sync interval in milliseconds (30000 = 30 seconds)
VITE_SYNC_INTERVAL=30000

# -----------------------------------------------------------------------------
# FEATURE FLAGS
# -----------------------------------------------------------------------------
# Enable AI-powered features (requires GEMINI_API_KEY)
VITE_ENABLE_AI=true

# Enable Google Sheets synchronization (requires VITE_GOOGLE_SHEET_URL)
VITE_ENABLE_SYNC=true

# Enable debug logging in console
VITE_DEBUG=false

# -----------------------------------------------------------------------------
# SECURITY CONFIGURATION (Recommended for Production)
# -----------------------------------------------------------------------------
# API rate limiting - requests per minute
VITE_API_RATE_LIMIT=60

# User session timeout in minutes
VITE_SESSION_TIMEOUT=60

# -----------------------------------------------------------------------------
# ANALYTICS & MONITORING (Optional - Recommended for Production)
# -----------------------------------------------------------------------------
# Google Analytics 4 Measurement ID
# Example: G-XXXXXXXXXX
VITE_GA_MEASUREMENT_ID=

# Sentry DSN for error tracking and monitoring
# Get from: https://sentry.io
VITE_SENTRY_DSN=

# LogRocket App ID for session replay and debugging
# Get from: https://logrocket.com
VITE_LOGROCKET_APP_ID=

# -----------------------------------------------------------------------------
# PAYMENT INTEGRATION (Optional - Future Enhancement)
# -----------------------------------------------------------------------------
# Stripe Publishable Key (for payment processing)
# Get from: https://dashboard.stripe.com/apikeys
VITE_STRIPE_PUBLISHABLE_KEY=

# PayPal Client ID (for PayPal payments)
# Get from: https://developer.paypal.com
VITE_PAYPAL_CLIENT_ID=

# -----------------------------------------------------------------------------
# EMAIL SERVICE (Optional - Future Enhancement)
# -----------------------------------------------------------------------------
# SendGrid API Key (for sending invoice emails)
# Get from: https://app.sendgrid.com/settings/api_keys
VITE_SENDGRID_API_KEY=

# SMTP Configuration (alternative to SendGrid)
VITE_SMTP_HOST=smtp.gmail.com
VITE_SMTP_PORT=587
VITE_SMTP_USER=
VITE_SMTP_PASS=

# -----------------------------------------------------------------------------
# CLOUD STORAGE (Optional - Future Enhancement)
# -----------------------------------------------------------------------------
# AWS S3 Configuration (for file/receipt storage)
VITE_AWS_REGION=us-east-1
VITE_AWS_BUCKET=
VITE_AWS_ACCESS_KEY_ID=
VITE_AWS_SECRET_ACCESS_KEY=

# Cloudinary Configuration (for image uploads and management)
# Get from: https://cloudinary.com/console
VITE_CLOUDINARY_CLOUD_NAME=
VITE_CLOUDINARY_API_KEY=
VITE_CLOUDINARY_API_SECRET=

# -----------------------------------------------------------------------------
# PRODUCTION ENVIRONMENT VARIABLES
# -----------------------------------------------------------------------------
# For production deployment, set these on your hosting platform:
#
# Required:
# - GEMINI_API_KEY
# - NODE_ENV=production
#
# Highly Recommended:
# - VITE_GOOGLE_SHEET_URL (for data backup)
# - VITE_SENTRY_DSN (for error monitoring)
# - VITE_GA_MEASUREMENT_ID (for analytics)
#
# Optional:
# - Payment integration keys (if accepting payments)
# - Email service keys (if sending automated emails)
# - Cloud storage keys (if storing files)
# -----------------------------------------------------------------------------

# =============================================================================
# SECURITY NOTES
# =============================================================================
# 1. NEVER commit .env.local or .env.production to version control
# 2. Add .env*.local to your .gitignore file
# 3. Use different API keys for development, staging, and production
# 4. Rotate API keys regularly
# 5. Use environment-specific keys with appropriate permissions
# 6. For production, use your hosting platform's environment variable system
# 7. All VITE_ prefixed variables are exposed to client-side code
# 8. Store sensitive server-side keys without VITE_ prefix when possible
# =============================================================================

# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your GEMINI_API_KEY (required)
# 3. Optionally configure Google Sheets URL for cloud backup
# 4. Adjust feature flags as needed
# 5. For production, set environment variables on your hosting platform
# =============================================================================
